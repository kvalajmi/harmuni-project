# 🔐 بيانات تطبيق المهام (Harmuni Task) - سري جداً

> ⚠️ **تحذير**: هذا الملف يحتوي على مفاتيح سرية. احفظه في مكان آمن ولا تشاركه مع أي شخص!

---

## 🌐 معلومات السيرفر (VPS)
| البيان | القيمة |
| :--- | :--- |
| المزود | DigitalOcean |
| IP | `167.99.241.118` |
| المنطقة | FRA1 (فرانكفورت) |
| المستخدم | `root` |
| كلمة المرور | `H@rmuni_2026_SuP3R!` |
| الذاكرة | 1 GB RAM |

### أوامر الدخول:
- **Host:** `167.99.241.118`
- **Username:** `root`
- **Password:** `Wwifzin-q73183` (Updated on 2026-01-10)
- **Port:** `22`

---

## 🗄️ Supabase (قاعدة البيانات)
| البيان | القيمة |
| :--- | :--- |
| Project URL | `https://wefjsxugozhhzubdtpeb.supabase.co` |
| Anon Key | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndlZmpzeHVnb3poaHp1YmR0cGViIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY4ODMyNDAsImV4cCI6MjA4MjQ1OTI0MH0.Ef2OEF0hbxpE6JpyFGNhhkdNSoQ2_ec6ixSMnFqePHc` |
| Service Role Key | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndlZmpzeHVnb3poaHp1YmR0cGViIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2Njg4MzI0MCwiZXhwIjoyMDgyNDU5MjQwfQ.FpHEEURuxB0CY5bsT329F-bUUf6lYYCptUlWYsuwR4Q` |
| Dashboard | https://supabase.com/dashboard/project/wefjsxugozhhzubdtpeb |

---

## 🔔 OneSignal (الإشعارات)
| البيان | القيمة |
| :--- | :--- |
| App ID | `6d710068-0d52-4ca5-8aa2-79d89d525c27` |
| REST API Key | `os_v2_app_nvyqa2ankjgklcvcphmj2us4e767ntwyf4lu5o5u4ujixrlk6bsp5egys46zvwjqzxyhqan3k2psu2fcktoirnvpg7ezrwlncbzltbq` |
| Dashboard | https://dashboard.onesignal.com/apps/6d710068-0d52-4ca5-8aa2-79d89d525c27 |

---

## 📧 Resend (البريد الإلكتروني)
| البيان | القيمة |
| :--- | :--- |
| API Key | `re_NZJu2fe8_Q9Fo3vGDVC1UYxQhnFPUiUKW` |
| Dashboard | https://resend.com/emails |

---

## ☁️ Cloudflare (DNS & SSL)
| البيان | القيمة |
| :--- | :--- |
| الدومين | `harmuni.org` |
| SSL Mode | Flexible |
| حساب | `Kvalajmi@gmail.com` |
| Dashboard | https://dash.cloudflare.com |

---

## 🔗 روابط مهمة
| الخدمة | الرابط |
| :--- | :--- |
| الموقع | https://harmuni.org |
| صفحة الدخول | https://harmuni.org/login |
| DigitalOcean | https://cloud.digitalocean.com |
| GitHub Repository | https://github.com/kvalajmi/harmuni-project |
| GitHub Actions | https://github.com/kvalajmi/harmuni-project/actions |
| Docker Hub | https://hub.docker.com/r/kvalajmi/harmuni-task |

## 📚 ملفات التوثيق المهمة

| الملف | الوصف |
| :--- | :--- |
| `AI_WORKFLOW.md` | **دليل شامل للذكاء الاصطناعي** - اقرأه أولاً! |
| `DEPLOY.md` | دليل النشر السريع والـ CI/CD |
| `تطبيق_المهام_البيانات_المهمة.md` | هذا الملف - كلمات السر والتوثيق |

> ⚠️ **مهم للـ AI:** اقرأ `AI_WORKFLOW.md` أولاً قبل أي تعديل!

---

## 📁 ملف Environment Variables (للسيرفر)
```env
NEXT_PUBLIC_SUPABASE_URL=https://wefjsxugozhhzubdtpeb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndlZmpzeHVnb3poaHp1YmR0cGViIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY4ODMyNDAsImV4cCI6MjA4MjQ1OTI0MH0.Ef2OEF0hbxpE6JpyFGNhhkdNSoQ2_ec6ixSMnFqePHc
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndlZmpzeHVnb3poaHp1YmR0cGViIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2Njg4MzI0MCwiZXhwIjoyMDgyNDU5MjQwfQ.FpHEEURuxB0CY5bsT329F-bUUf6lYYCptUlWYsuwR4Q
RESEND_API_KEY=re_NZJu2fe8_Q9Fo3vGDVC1UYxQhnFPUiUKW
CRON_SECRET=harmuni-cron-secret-2024
```

---

## 📝 سجل التحديثات والإصلاحات

### 2026-01-09: إضافة توثيق شامل للذكاء الاصطناعي

**الهدف:**
- توثيق كامل للعمل مع نماذج الذكاء الاصطناعي
- ضمان الاستمرارية عند التبديل بين نماذج AI مختلفة
- دليل خطوة بخطوة لأي AI يعمل على المشروع

**الملف الجديد:**
- `AI_WORKFLOW.md` - دليل شامل للـ AI يحتوي على:
  - معلومات البنية التحتية الكاملة
  - خطوات العمل مع المستخدم
  - قواعد يجب اتباعها (Do's & Don'ts)
  - أمثلة على طلبات شائعة
  - Checklist قبل أي تعديل
  - الإصلاحات الشائعة

**التحديثات:**
- تحديث `تطبيق_المهام_البيانات_المهمة.md` بإضافة روابط وملفات التوثيق

**Commit:**
- `bffda58` - Docs: Add comprehensive AI workflow documentation

**الفائدة:**
- ✅ أي AI جديد يقدر يفهم المشروع بسرعة
- ✅ استمرارية العمل بين جلسات مختلفة
- ✅ تقليل الأخطاء والتكرار
- ✅ توحيد طريقة العمل

---

### 2026-01-09: إعداد CI/CD للنشر السريع (GitHub Actions + Docker Hub)

**المشكلة:**
- النشر في السيرفر يأخذ 9-10 دقائق (docker build)
- السيرفر بطيء في البناء (1 GB RAM فقط)
- الكود في السيرفر كان قديم (مو git repo)

**الحل:**
- إعداد **GitHub Actions** لبناء تلقائي عند كل `git push`
- رفع الـ image المبني لـ **Docker Hub**
- السيرفر يسحب الـ image الجاهز (10-20 ثانية فقط!)

**الملفات الجديدة:**
- `.github/workflows/deploy.yml` - CI/CD pipeline
- `docker-compose.prod.yml` - Production config
- `update-server.sh` - سكريبت نشر سريع للسيرفر
- `DEPLOY.md` - دليل النشر الكامل

**كيف تستخدمه:**
1. على جهازك: `git push` (GitHub Actions يبني تلقائياً)
2. انتظر 3-4 دقائق (GitHub يبني)
3. في السيرفر: `./update-server.sh` (10-20 ثانية!)

**الفوائد:**
- ✅ 27x أسرع (20 ثانية بدل 9 دقائق!)
- ✅ البناء يصير في GitHub (أسرع)
- ✅ السيرفر فقط يسحب image جاهز
- ✅ نشر تلقائي وسهل

**المتطلبات:**
- Docker Hub account: `kvalajmi`
- GitHub Secrets: `DOCKER_USERNAME` و `DOCKER_TOKEN`

**Commit:**
- `00179f3` - CI/CD: Setup GitHub Actions + Docker Hub deployment

---

### 2026-01-09: إصلاح مشكلتين في مهام الموظفين

**المشكلة 1: المهام لا تظهر للموظف الذي أنشأها**
- الموظف الذي ينشئ مهمة لا يراها في قائمته لمتابعتها
- السبب: استخدام `!inner` join في الاستعلام

**الحل:**
- تغيير من `!inner` إلى left join في `getUserEmployeeTasksAction`
- الآن المنشئ يشوف المهام اللي أنشأها حتى لو ما assigned لنفسه

**المشكلة 2: حقل الموعد النهائي غير مطلوب**
- المستخدم لا يحتاج حقل "الموعد النهائي" في نموذج إنشاء المهمة

**الحل:**
- إزالة حقل `dueDate` من `CreateEmployeeTaskDrawer`
- إزالة state والـ input field
- إرسال `null` دائماً لـ due_date parameter

**الملفات المعدلة:**
- `src/lib/employee-task-actions.ts` - إصلاح الاستعلام (إزالة !inner)
- `src/components/create-employee-task-drawer.tsx` - إزالة حقل الموعد النهائي

**Commit:**
- `ffdb897` - Fix: Show created tasks to creator + Remove due date field

---

### 2026-01-09: تحسين أداء قسم مهام الموظفين (التحديث الثاني - تحسين الاستعلامات)

**المشكلة:**
- بطء في تحميل قسم "مهام الموظفين" حتى بعد إضافة SWR
- الاستعلامات تجلب كل البيانات بدون حد (unlimited records)
- جلب كل التعليقات فقط للعد comment_count
- جلب Activities غير ضرورية في القائمة الرئيسية

**الحل:**
- إضافة `.limit(100)` للاستعلامات (أحدث 100 مهمة فقط)
- استخدام `employee_task_comments(count)` بدلاً من جلب كل التعليقات
- حذف `employee_task_activities` من الاستعلام الرئيسي
- تقليل حجم البيانات المنقولة من قاعدة البيانات

**الملفات المعدلة:**
- `src/lib/employee-task-actions.ts` - تحسين استعلامات getUserEmployeeTasksAction و getAllEmployeeTasksAction

**Commits:**
- `81a0855` - Performance: Optimize database queries for employee tasks

**الفوائد:**
- ✅ سرعة أكبر في تحميل البيانات من قاعدة البيانات
- ✅ تقليل الحمل على السيرفر والداتابيز
- ✅ أداء أفضل مع زيادة عدد المهام (scalable)
- ✅ نقل بيانات أقل عبر الشبكة

---

### 2026-01-09: تحسين أداء قسم مهام الموظفين (التحديث الأول - SWR Caching)

**المشكلة:**
- بطء في تحميل قسم "مهام الموظفين" (Employee Tasks)
- استعلامات معقدة مع JOINs متعددة بدون caching
- استخدام useEffect/useState بدلاً من SWR

**الحل:**
- إضافة hook جديد `useEmployeeTasksList` في `src/lib/hooks.ts`
- تحديث `EmployeeTasksTab.tsx` لاستخدام SWR بدلاً من الطريقة القديمة
- تفعيل automatic caching و deduplication للطلبات
- استخدام stale-while-revalidate pattern لتحسين UX

**الملفات المعدلة:**
- `src/lib/hooks.ts` - إضافة useEmployeeTasksList hook
- `src/components/dashboard/EmployeeTasksTab.tsx` - تحديث لاستخدام SWR
- `src/lib/supabase-browser.ts` - تنظيف الكود (إزالة EOF artifacts)

**Commits:**
- `7c9229c` - Performance: Optimize employee tasks loading with SWR caching

**الفوائد:**
- ✅ تحميل أسرع للبيانات من الـ cache
- ✅ تقليل عدد الطلبات للسيرفر
- ✅ تحديثات تلقائية للبيانات بعد العمليات (إغلاق/إنشاء مهام)

---

### 2026-01-09: إصلاح مشكلة تسجيل الدخول (this.lock error)

**المشكلة:**
- خطأ `TypeError: this.lock is not a function` يمنع تسجيل الدخول
- المشكلة في Web Locks API في PWA/بعض المتصفحات

**الحل:**
- تم تعديل `src/lib/supabase-browser.ts`
- تغيير من `createBrowserClient` إلى `createClient` مباشرة
- تعطيل lock function: `lock: async (_name, _acquireTimeout, fn) => fn()`

**التغييرات على السيرفر:**
- ⚠️ **كلمة مرور SSH تم تغييرها** - استخدم كلمة مرور جهازك للدخول الآن
- تم تفعيل PasswordAuthentication في SSH config: `/etc/ssh/sshd_config`

**أوامر الدخول للسيرفر (محدثة):**
```bash
# عبر DigitalOcean Console (الطريقة الموصى بها):
# https://cloud.digitalocean.com -> Droplets -> harmuni-task -> Access -> Launch Droplet Console

# عبر SSH (تحتاج كلمة مرور جهازك):
ssh root@167.99.241.118
```

**أوامر إعادة نشر التحديثات:**
```bash
# داخل السيرفر عبر DigitalOcean Console:
cd /app
docker compose down
docker compose build --no-cache
docker compose up -d
```

**الملفات المعدلة:**
- `src/lib/supabase-browser.ts` - الإصلاح الرئيسي

**Commits:**
- `82a8841` - Fix: Resolve this.lock error in Supabase browser client
- `e573791` - Fix: Use createClient directly to resolve this.lock error

---

### 2026-01-10: إصلاح مشكلة Dashboard الإنتاج (Skeleton Screens + Lock Issue)

**المشكلة:**
- Dashboard يظل معلق على skeleton screens (مربعات رمادية)
- المشكلة بعد deployment على `harmuni.org`

**الأسباب المكتشفة:**
1. **Loading Logic خاطئ**: الشرط `(!profile && !user)` كان يخفي الـ skeletons قبل تحميل البيانات
2. **Supabase Lock Hang**: `supabase.auth.getSession()` كان معلق بسبب Web Locks API
3. **Server Action ID Mismatch**: Cloudflare CDN كان يحفظ JavaScript قديم بـ Server Action IDs مختلفة

**الحلول:**
1. **تصحيح Loading Logic** في `src/app/dashboard/page.tsx`:
   ```typescript
   const loading = authLoading || (user && !profile)
   ```
2. **إضافة Custom Lock Function** في `src/lib/supabase-browser.ts`:
   ```typescript
   lock: async (_name, _acquireTimeout, fn) => await fn()
   ```
3. **Cloudflare Cache Purge**: مسح كامل للـ CDN cache لتحميل JavaScript الجديد

**الملفات المعدلة:**
- `src/app/dashboard/page.tsx` - إصلاح loading state
- `src/lib/supabase-browser.ts` - إضافة custom lock function
- `src/lib/auth-context.tsx` - تحسين error handling والـ debug logs

**خطوات النشر:**
1. إصلاح الكود محلياً
2. رفع الملفات للسيرفر عبر `scp`
3. إعادة بناء Docker image بـ `--no-cache`
4. مسح Cloudflare cache (Purge Everything)
5. Hard refresh في المتصفح (`Cmd+Shift+R`)

**النتيجة:**
- ✅ Dashboard يعمل 100% بعد cache purge
- ✅ Profile loading سريع وبدون تعليق
- ✅ Authentication يعمل بشكل صحيح
- ✅ Build ID الجديد: `7C25we7W2CM83nrr3PlPY`

---

### 2026-01-10: إضافة Push Notifications لمهام الموظفين

**المشكلة:**
- إشعارات OneSignal لا تُرسل عند إنشاء مهام بين الموظفين
- التعليقات على المهام لا تُرسل إشعارات
- إغلاق المهام لا يُشعر المشاركين

**الحل:**
- إضافة `sendPushNotificationAction` calls في `src/lib/employee-task-actions.ts`
- إشعارات لجميع الحالات:
  1. **إنشاء مهمة** → إشعار للمرسَل لهم + إشعار للمدير (إذا المنشئ موظف)
  2. **تعليق على مهمة** → إشعار لجميع المشاركين (المنشئ + المكلّفين)
  3. **إغلاق مهمة** → إشعار للجميع

**الملفات المعدلة:**
- `src/lib/employee-task-actions.ts` - إضافة push notifications لـ:
  - `createEmployeeTaskAction` (سطر 135-189)
  - `addEmployeeTaskCommentAction` (سطر 413-428)
  - `closeEmployeeTaskAction` (سطر 506-521)

**التحديثات:**
- استخدام `title` و `body` بدلاً من `heading` و `content` (TypeScript fix)
- إضافة admin notification عند إنشاء مهام بين الموظفين
- Error handling لضمان عدم فشل العملية إذا فشل الإشعار

**النتيجة:**
- ✅ إشعارات تعمل لجميع الحالات
- ✅ المدير يتلقى إشعارات عند تفاعل الموظفين
- ✅ جميع المشاركين يتلقون تحديثات فورية

---

## 🔧 معلومات السيرفر المحدثة (2026-01-10)

### مسارات المشروع على VPS:
| المسار | الوصف |
| :--- | :--- |
| `/app` | **Harmuni Task** (نظام المهام) - Port 3000 |
| `/root/attendance-system` | نظام الحضور - Port 3001 |

### Docker Containers:
| Container | Port | Status |
| :--- | :--- | :--- |
| `harmuni-task` | 3000 | ✅ Running |
| `attendance-app` | 3001 | ⏸️ Stopped (حالياً) |

### Nginx Configuration:
- `harmuni.org` → Port 3000 (Task Management) ✅
- `attendance.harmuni.org` → Port 3001 (Attendance)

### أوامر مهمة:
```bash
# إعادة بناء وتشغيل
cd /app
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml build --no-cache
docker compose -f docker-compose.prod.yml up -d

# فحص الحالة
docker ps
docker logs harmuni-task --tail 50

# فحص Environment Variables
docker exec harmuni-task printenv | grep NEXT_PUBLIC
```

---

📅 تاريخ الإنشاء: 2026-01-05
📅 آخر تحديث: 2026-01-10 (23:55)
